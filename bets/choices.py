disciplines = ((id, title) for id, title in enumerate(["Football"], 1))

types = ((id, title) for id, title in enumerate(["Single"], 1))

statuses = (
    (id, title) for id, title in enumerate(["In progress", "Success", "Lost"], 1)
)
